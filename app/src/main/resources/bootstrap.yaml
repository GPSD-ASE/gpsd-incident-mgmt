spring:
  cloud:
    vault:
      uri: ${VAULT_ADDR}
      authentication: kubernetes
      kubernetes:
        role: ${VAULT_ROLE}
        service-account-token-file: ${VAULT_TOKEN}
      kv:
        enabled: true
        backend: secret
        default-context: gpsd/incident-mgmt
        application-name: gpsd/incident-mgmt