spring:
  cloud:
    vault:
      uri: ${VAULT_ADDR}
      authentication: kubernetes
      kubernetes:
        role: ${VAULT_ROLE}
      kv:
        enabled: true
        backend: secret
        default-context: gpsd/incident-mgmt
        application-name: gpsd/incident-mgmt
      token: ${VAULT_TOKEN}